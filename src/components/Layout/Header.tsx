const Header = () => {
  return (
    <header className="flex justify-center items-center gap-4">
      <button>Login</button>
      <button>Register</button>
    </header>
  );
};

export default Header;
